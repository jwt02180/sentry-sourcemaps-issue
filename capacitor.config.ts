import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sentry-sourcemaps-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
